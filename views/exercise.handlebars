{{> navbar}}
<h1>{{label}}</h1>
<div class="row">
	<div class="col-md-4">
		<div class="card" style="width: 18rem; background-color: white;">
		  <div class="card-body">
		    {{#each exercise}}
		      {{> exercise}}
		    {{/each}}
		  </div>
		</div>
	</div>
	{{#if chart}}
		{{> chart}}
	{{/if}}
</div>
{{#if modal}}
	{{> modal}}
{{/if}}

